---
# optimizer options
batch_size: 128
optimizer: 'SGD'
learning_rate: 0.1
momentum: 0.9
l2_reg: 0.0001
dropout: 0

# lr scheduler options
lr_milestones:
  - 80
  - 100
lr_gamma: 0.1

# training options
epochs: 120

# run options
gpu: 0
no_gpu: false
model: "LSTM"
dataset_root: "data/normalized/"
num_workers: 4
no_memory_pinning: false
channels_first: false
as_greyscale: false

# experimental options
subsampling: false
subsample_size: 100
no_clipping: true

# dataset one-shot preprocessing options
# size of window to average sequences over
temp_ds_wnd: 2

# dataset options
seq_len: 100
stride: 2
sequential_targets: false


# misc options
log_root: "logs/"
run_name: "lstm"


